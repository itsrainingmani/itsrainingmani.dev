---
import { formatDate } from "@/utils";
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="text-base text-gray-600" datetime={date.toISOString()}>
	{formatDate(date)}
</time>
