---
import { formatDate } from "@/utils";
interface Props {
	date: Date;
}

const { date } = Astro.props;
const isoDate = date.toISOString();
---

<time class="text-base text-gray-600" datetime={isoDate}>
	{formatDate(isoDate)}
</time>
